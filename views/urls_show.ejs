<!DOCTYPE html>
<html lang="en">
<head>
    <% include partials/_header %>
</head>
<body>
    <main>  
        <div> 
            <h1>This is urls_show</h1>
                <ul> 
                <% if (username) { %> 
                    <%= longURL %> ---
                    <%= shortURL %>
                    <form method="POST" action="/urls/<%= urls[username][index].shortURL %>/update">
                    New URL: <input type="text" name="newURL">
                    <input type="submit" value="UPDATE"></form>
                <% } else { %>
                    Please login to update URLs
                <% } %>
                </ul>
        </div>
    </main> 
</body>
</html>